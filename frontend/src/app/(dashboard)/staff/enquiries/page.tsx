'use client';
// Same screen as the admin's: the backend scopes enquiries to the gym and
// hides convert/delete from staff, so one page serves both.
export { default } from '@/app/(dashboard)/admin/enquiries/page';
